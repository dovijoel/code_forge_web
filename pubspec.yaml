name: code_forge
description: "A new Flutter package project."
version: 0.0.1
homepage:

environment:
  sdk: ^3.10.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter  
  http: ^1.6.0
  markdown_widget: ^2.3.2+8
  meta: ^1.17.0
  re_highlight: ^0.0.3
  web_socket_channel: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
